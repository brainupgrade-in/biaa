# Building Intelligent AI Agents - OPTIONAL requirements
#
# Intentionally empty. What used to live here (transformers + tf-keras) is now
# part of the CORE requirements -- the Hugging Face / transformer labs on Day 2
# use a real pre-trained model, so those packages are essential, not extras.
# See scripts/requirements-core.txt (Tier 2, section 3d).
#
# This file is kept as a back-compat placeholder so the setup scripts and the
# `--with-hf` flag keep working (they now install nothing extra). There are no
# genuinely-optional packages at the moment; add any here if that changes.
